/**
 * / Theme
 */

export type Theme = {
	text: string;
	background: string;
	border: string;
};

export const LightTheme: Theme = { text: 'text-neutral', background: 'bg-neutral', border: 'border-neutral' };
